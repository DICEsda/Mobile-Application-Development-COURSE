<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy.

  General traffic (OpenLibrary, Firebase, etc.) must use HTTPS. Cleartext HTTP
  is permitted ONLY for the local LM Studio server on loopback-style hosts used
  by the Book Companion feature:
    - 10.0.2.2  : the Android emulator's alias for the host machine's localhost
    - localhost / 127.0.0.1 : reachable on a physical device via
        `adb reverse tcp:1234 tcp:1234`

  A local LLM server is HTTP by nature, so this scoped exception is preferable
  to enabling cleartext globally. To use a LAN IP directly from a physical
  device, add that IP as another <domain> entry here.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
